cron
point entrée url de la source a changé
stockage données dans une base de données mysql (essayer de voir si ça convient). Peut être utiliser keyof pour récupérer les klés des JSON et stocker ensuite les données dans des tables différentes

réapprendre la structure du projet
quelle données renvoyer quand les méthodes on été un succès (exemple parser)
faire de la branche Parser la branch Master (database, unzipper, config sont des branches enfants de parser)
pourquoi le script unzip fonctione dans TS et pas dans ./dist ?

note 20/12/22 : remotesql est actuellement indisponible